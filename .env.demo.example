# ReclaimAI demo environment template
# Copy to .env.demo and replace placeholders before hosting publicly.

DEMO_HTTP_PORT=8088
PUBLIC_BASE_URL=http://localhost:8088

NODE_ENV=production

POSTGRES_USER=reclaimai
POSTGRES_PASSWORD=reclaimai_demo_password
POSTGRES_DB=reclaimai

DATABASE_URL=postgresql://reclaimai:reclaimai_demo_password@postgres:5432/reclaimai
REDIS_URL=redis://redis:6379/0
KAFKA_BOOTSTRAP_SERVERS=redpanda:9092

PETPOOJA_WEBHOOK_SECRET=change-me-demo-petpooja-secret
DEFAULT_CASHBACK_AMOUNT_INR=100
WEBHOOK_IDEMPOTENCY_TTL_SECONDS=604800
SHOWCASE_PETPOOJA_REST_ID=pp_out_88219
DEMO_SIMULATE_RATE_LIMIT_MAX=5
DEMO_SIMULATE_RATE_LIMIT_WINDOW_SECONDS=600
CORS_ORIGINS=http://localhost:8088

CLAIM_JWT_SECRET=change-me-demo-claim-jwt-secret
PHONE_HASH_SECRET=change-me-demo-phone-hash-secret
ENVIRONMENT=production
# Hosted demo OTP via Resend email (no Mailhog). Set a real RESEND_API_KEY.
# Optional: OTP_PROVIDER=whatsapp + META_WA_OTP_TEMPLATE_NAME once an OTP template is approved.
OTP_PROVIDER=resend
RESEND_API_KEY=
OTP_FROM_EMAIL=ReclaimAI Demo <onboarding@resend.dev>
META_WA_OTP_TEMPLATE_NAME=
META_WA_OTP_TEMPLATE_LANG=en
# SMTP kept only for local docker-compose.dev.yml — unused when OTP_PROVIDER=resend
SMTP_HOST=
SMTP_PORT=
SMTP_FROM=

# Auth — hosted demo MUST use Auth0 Universal Login (bypass OFF).
# Create a shared Auth0 user; map tenancy.staff_users.auth0_sub to that user's sub
# (or leave seed as dev|demo-owner and match AUTH0_DEMO_USER_EMAIL so first login links).
AUTH_DEV_BYPASS=false
AUTH_DEV_SECRET=change-me-demo-auth-secret
AUTH0_DOMAIN=your-tenant.auth0.com
AUTH0_AUDIENCE=https://api.reclaimai.demo
AUTH0_JWKS_URL=
AUTH0_CLIENT_ID=your-auth0-spa-client-id
AUTH0_CLIENT_SECRET=
AUTH0_SECRET=
AUTH0_DEMO_USER_EMAIL=owner@demo.reclaimai.local
AUTH0_DEMO_USER_SUB=auth0|replace-with-shared-demo-user-sub
NEXT_PUBLIC_AUTH_DEV_BYPASS=false
NEXT_PUBLIC_AUTH0_DOMAIN=your-tenant.auth0.com
NEXT_PUBLIC_AUTH0_CLIENT_ID=your-auth0-spa-client-id
NEXT_PUBLIC_AUTH0_AUDIENCE=https://api.reclaimai.demo
NEXT_PUBLIC_AUTH0_CALLBACK_URL=http://localhost:8088/dashboard
NEXT_PUBLIC_POS_API_URL=/api/pos
NEXT_PUBLIC_IDENTITY_API_URL=/api/identity

# RazorpayX — hosted demo uses test-mode API (real HTTP + webhooks, no live money).
# Service refuses to start with RAZORPAYX_MOCK=false unless key id/secret, account number,
# and webhook secret are set. Compose also forces RAZORPAYX_MOCK=false.
# Public webhook (Caddy): {PUBLIC_BASE_URL}/api/identity/v1/webhooks/razorpayx/payout
RAZORPAYX_KEY_ID=rzp_test_replace_me
RAZORPAYX_KEY_SECRET=replace-with-razorpayx-test-key-secret
RAZORPAYX_ACCOUNT_NUMBER=replace-with-razorpayx-test-account-number
RAZORPAYX_WEBHOOK_SECRET=change-me-demo-razorpayx-webhook-secret
RAZORPAYX_MOCK=false
RAZORPAYX_BASE_URL=https://api.razorpay.com/v1

# WhatsApp — hosted demo uses real Meta Cloud API (no in-process mock).
# Service refuses to start with WHATSAPP_MOCK=false unless token + phone number ID are set.
# Compose also forces WHATSAPP_MOCK=false and WHATSAPP_SEND_MODE=template.
WHATSAPP_MOCK=false
WHATSAPP_SEND_MODE=template
META_WA_TOKEN=replace-with-meta-cloud-api-token
META_WA_PHONE_NUMBER_ID=replace-with-meta-phone-number-id
META_WA_VERIFY_TOKEN=change-me-demo-meta-verify-token
# Approved template name; body vars: {{1}}=message_body {{2}}=discount {{3}}=cta_url (optional)
META_WA_TEMPLATE_NAME=reclaimai_offer
META_WA_TEMPLATE_LANG=en
META_WA_API_VERSION=v21.0
# Public webhook (Caddy): {PUBLIC_BASE_URL}/api/whatsapp/v1/webhooks/meta-whatsapp
# Gupshup optional — leave empty for portfolio (Meta primary only)
GUPSHUP_API_KEY=
GUPSHUP_APP_NAME=
GUPSHUP_SOURCE_NUMBER=
GUPSHUP_API_URL=https://api.gupshup.io/wa/api/v1/msg
WHATSAPP_PROVIDER_TIMEOUT_MS=10000
DISPATCH_IDEMPOTENCY_TTL_SECONDS=1209600

ANTHROPIC_API_KEY=

KAFKA_ENABLED=true
OFFER_SCHEDULE_DELAY_HOURS=24
